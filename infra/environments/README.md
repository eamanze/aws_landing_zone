# Terraform Environment Roots

Each subdirectory is reserved for one independently initialized and state-isolated Terraform root.

The initial directories contain documentation only. A root becomes deployable only after it has real configuration, a copied `versions.tf`, an approved backend key, an execution-role design, and an ownership review against AWS Control Tower.
