# Terraform Templates

`versions.tf` is the canonical Terraform and AWS provider constraint. Copy it unchanged into each implemented module and environment root, then commit that root's generated `.terraform.lock.hcl`.

This directory is not deployable.
