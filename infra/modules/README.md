# Terraform Modules

Each subdirectory is a placeholder for one reusable module defined by `AGENTS.md`.

Before a module becomes deployable, add its own `versions.tf`, inputs, outputs, tests, examples, ownership statement, and documentation. Do not add placeholder resources merely to make a plan non-empty.
