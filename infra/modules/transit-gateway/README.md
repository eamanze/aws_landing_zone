# transit-gateway

**Status:** scaffold only; no Terraform resources exist.

Optional shared network hub and isolated route-table model.

Before implementation, document the resource owner, deployment account, Regions, state consumer, inputs, outputs, tests, cost impact, and Control Tower overlap.
