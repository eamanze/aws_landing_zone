# Validation Evidence

Generated evidence is ignored by default because it may contain account identifiers, ARNs, security findings, or other sensitive operational data.

Commit only sanitized, reviewed evidence. Never commit credentials, session tokens, raw Terraform plans, or unredacted security findings.
